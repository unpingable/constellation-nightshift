schema = "project.observation-binding/v1"
producer = "example-fixture.status"
output_schema = "project.ops.status/v1"
kind = "exec/v1"
argv = ["python3", "producer.py", "literal;not-shell"]
environment = { FIXTURE_MODE = "bounded" }
