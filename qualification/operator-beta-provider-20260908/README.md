# Beta provider integration candidate

These are explicitly deterministic mapper fixtures, not model executions.
Generated by Switchyard2207bb6ca19690a2c218fffe6c69e429eee24eb5's
`qualification/provider_mapper_fixtures.py`, producer SHA256
554a6e2e537d85c2f396844da5e0d61da0143a9ea978e4aacd6800f640a12368,
against the exact beta Codex source6893ae42233aa95b1c1623497405681d99a589da.
Tests retarget explicit local dispatch identities and replay the complete mapper
graph; fixture generation needs neither classic implementations nor credentials.
Approval fixture intentionally has an open acquisition cut. The existing owner
may retain its nonterminal WAITING_APPROVAL evidence; it is not completed work
and never authorizes an approval response. Historical provider fixtures/schema
remain unchanged.

The CLI uses the existing Foreman owner: `provider-seal-inputs` only canonicalizes
operator-authored inputs; `provider-admit` enrolls local agent-compute constraints;
`provider-prepare` atomically opens one exact dispatch and V3 request. Run the
enrolled Switchyard adapter separately. `provider-derive-evidence --requirement
... --dispatch ... --snapshot ... --policy ... --received-at ... --expires-at ...`
derives and validates existing observation/disposition/optional park records.
The explicit `graph_validation` result must be `VALIDATED` before attempting
owner intake. `REFUSED` retains translated source evidence but is not an admitted
record or closed work attempt. In particular, the existing whole-second park
protocol refuses subsecond hints; policy exhaustion likewise must not erase a
valid provider refusal. Neither case permits an automatic second execution.
Optional `--history` supplies the exact existing deferral chain. `provider-record`
performs durable owner intake. Derivation never retries or authorizes another
dispatch, and provider completion is not a worker-result acceptance receipt.

Old source/schema pairs remain available for historical replay; the beta is an
explicit separately enrolled tuple, never an automatic fallback. The historical
deterministic-suite hash in V3 refers only to that retained regression input,
not to newly inherited runtime qualification. Actual beta lifecycle evidence and
independent review are required before release acceptance.
